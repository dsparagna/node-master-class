let config = {};

config = {
        interval: 2000,
        first_name: "Doug",
        last_name: "Sparagna",
        message: "This will keep the loop going indefinetly"
    }

module.exports = config;